This is just a simple sandbox. It's a great start for other images or just to experiment with something.

Usage:
vagrant up

When the installation is complete you can log in using:
vagrant ssh

The vagrant user has sudo rights.
