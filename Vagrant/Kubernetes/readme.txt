Kubernetes is a container orchestrator tool for automating application deployment, and management.

This Vagrant image installs a one node Kubernetes "cluster" into a virtual machine.
Usage:
vagrant up

When the installation is complete you can log in using:
vagrant ssh

The vagrant user has sudo rights.
